<h1>Available decks</h1>

<ul>
<% decks.each do |deck| %>
  <li>
    <a href="<%= static_decks_path %>/<%= deck.slug %>/index.html" title="<%= deck.name %>"><%= deck.name %></a>
  </li>
<% end %>
</ul>
